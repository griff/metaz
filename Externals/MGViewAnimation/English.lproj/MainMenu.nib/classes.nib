{
    IBClasses = (
        {
            ACTIONS = {animate = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {checkbox = NSButton; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}